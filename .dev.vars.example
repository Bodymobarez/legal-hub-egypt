# Copy to `.dev.vars` for `wrangler dev` (gitignored — same rules as .env).
# Production: use `wrangler secret put DATABASE_URL` and `wrangler secret put SESSION_SECRET`.

DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
SESSION_SECRET=change-me-to-a-long-random-string
